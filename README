To run the disassembler:

You will need Java installed to run this!
You will need some x86 64 bit ELF file to test with.

1. Locate interim.jar, open a new terminal and cd to its location

2. Make sure you know the location of some ELF file that you wish to disassemble

3. type:    "java -classpath interim.jar first.first"

4. you will be prompted to input the path to the ELF file

5. Make sure:
    - path is correct + file exists
    - the file to disassembler is a x86_64 ELF

6. Disassembled instructions will display
